import React  from 'react';
// import Dashboard from '../Dashboard';
import Auth from '../Auth';

const App = (props: any) => {
    return <Auth />
    /*
    if (!isAuth) return <Auth />;
        return (
            <Dashboard />
        );
    */
};

export default App;
