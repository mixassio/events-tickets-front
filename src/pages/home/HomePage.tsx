import React from 'react';
import Typography from "@material-ui/core/Typography";

const Home = () => {
    return (
        <div>
            <Typography>
                Events-tickets
            </Typography>
        </div>
    );
};

export default Home;
