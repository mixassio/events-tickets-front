import React from 'react';
import Typography from "@material-ui/core/Typography";

const NotFound = () => {
    return (
        <Typography>
            404
        </Typography>
    );
};

export default NotFound;
